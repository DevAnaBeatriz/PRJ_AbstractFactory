package buttons;

/**
 * Interface do produto abstrato Button. 
 * Define uma família de objetos relacionados (botões), que podem variar em sua
 * implementação, mas compartilham a mesma interface.
 */
public interface Button {
    void paint();
}
